@page "{id:int}"
@model CleanTasks.RazorGUI.Pages.Tasks.DeleteModel
@{
    ViewData["Title"] = "Delete area";
}

<main class="container-fluid main-container">
    <form class="card mt-2" style="max-width: 500px;" method="post">
        <input asp-for="@Model.Id" />
        <section class="card-header">
            Delete area
        </section>
        <section class="card-body">
            Are you sure you want to delete area with @Model.AreaName?
        </section>
        <section class="card-footer">
            <button type="submit" class="btn btn-danger">Delete</button>
            <a class="btn btn-secondary" asp-page="/Tasks/Workspace">Cancel</a>
        </section>
    </form>
</main>
